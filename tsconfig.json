{
  "extends": "expo/tsconfig.base",      
  "compilerOptions": {
    "strict": true,                     
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    },
    "jsx": "react-native",
  },
  // Folders (globs) TypeScript should include / exclude
  "include": ["app", "src", "tests"],  
  "exclude": ["node_modules", "android", "ios", ".expo"]
}
# DisasterCan

_Gamified disaster-preparedness for Canadians – built with Expo + React Native_

## Quick-start

```bash
# Clone & install
git clone git@github.com:<YOUR_USER>/disastercan.git
cd disastercan
npm install

# Start Expo
npx expo start        # press i (iOS) / a (Android) / w (web)
```
